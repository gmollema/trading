.env
.venv/
__pycache__/
*.pyc
*.egg-info/
trades.csv
logs/
open_positions.json
safety-check-log.json
watchlist.txt
backtest_data/

# SMC bot runtime state (mirrors the gap-and-go bot's ignored state files)
smc_trades.csv
smc_open_positions.json
smc-safety-check-log.json
smc_watchlist.txt

# UT Bot runtime state (mirrors the gap-and-go / SMC bots' ignored state files)
ut_bot_open_positions.json

# RSI(2) / RSI(20) dip runtime state. The rules JSONs ARE tracked -- they
# are configuration -- but positions, fills and heartbeats are per-machine
# state written by the cycles.
rsi2_open_positions.json
rsi2_trades.csv
rsi20_dip_open_positions.json
rsi20_dip_trades.csv
rsi20_dip_etf_open_positions.json
rsi20_dip_etf_trades.csv

# Heartbeat files written by cycle.py/smc_cycle.py for heartbeat_monitor.py
heartbeat.json
smc_heartbeat.json
rsi2_heartbeat.json
rsi20_dip_heartbeat.json
rsi20_dip_etf_heartbeat.json

# IDE / test cache / local Claude Code settings -- machine-specific, not shared
.idea/
.pytest_cache/
.claude/

# Generated live-trading dashboard (compute_perf.py writes this)
dashboard/

# Ad-hoc backtest/sweep experiment output at the project root -- scratch
# artifacts from running scripts, not source; every *.csv/*.log at root
# falls in this bucket (verified none exist under src/ or tests/)
*.csv
*.log
